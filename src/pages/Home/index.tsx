import MovieSearch from '../../components/MovieSearch';
import { StyledHomeContainer } from './styled';

const Home = () => {
  return (
    <StyledHomeContainer>
      <MovieSearch />
    </StyledHomeContainer>
  );
};

export default Home;
